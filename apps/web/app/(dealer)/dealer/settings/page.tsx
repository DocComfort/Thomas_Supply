import { Card, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function DealerSettingsPage() {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold tracking-normal">Settings</h1>
      <Card>
        <CardHeader>
          <CardTitle>Dealer settings placeholder</CardTitle>
          <CardDescription>Future settings for notifications, pickup preferences, and account-specific display controls.</CardDescription>
        </CardHeader>
      </Card>
    </div>
  );
}
